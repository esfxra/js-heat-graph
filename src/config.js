// Reference: https://github.com/laucyun/calendar-contributions-map

const config = {
  source: '', // json, google sheets
  data: [], // json data, google sheets URL
  colorRange: { noActivity: '', lessActivity: '' },
  dayWidth: 40,
  dayHeight: 40,
  selector: '#contributions-map',
};

export { config };
