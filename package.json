{
  "version": "0.0.0",
  "type": "module",
  "source": "src/index.html",
  "scripts": {
    "dev": "parcel",
    "build": "parcel build",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --no-cache"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^27.3.1",
    "parcel": "^2.0.1"
  },
  "dependencies": {
    "luxon": "^1.27.0"
  }
}
